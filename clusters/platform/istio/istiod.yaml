apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
  namespace: platform-istio
spec:
  interval: 10m
  dependsOn:
    - name: istio-base
  chart:
    spec:
      chart: istiod
      version: 1.28.3
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: platform-istio
  values:
    global:
      istioNamespace: platform-istio
