apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: upstream
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/icsimona/gitops-flow.git
  ref:
    branch: dev
